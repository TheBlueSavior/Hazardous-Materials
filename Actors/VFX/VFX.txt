Class HM_BulletHit : Actor
{
  //Original code done by Bugsie
  Default
  {
  Alpha 1.0;
  Speed 15;
  +BRIGHT
  +MISSILE
  +CLIENTSIDEONLY
  +BLOODLESSIMPACT 
  +FORCEXYBILLBOARD
  +THRUACTORS
  -NOGRAVITY
  +DONTSPLASH
  +THRUACTORS
  +NOBLOCKMONST
  +CANNOTPUSH
  +NOTRIGGER
  BounceFactor 1;
  BounceType "Hexen";
  RenderStyle "Add";
  Scale 0.6;
  Radius 0;
  Height 1;
  }
  states
  {
  Spawn:
    TNT1 A 0;
	NULL A 0
	{
	A_ScaleVelocity(frandom(0.3,0.6));
	A_SetScale(Scale.X-frandom(0.10,0.45));
	}
	NULL A 3;
	NULL A 1 A_FadeOut(0.105);
    Wait;
  }
}

Class HM_HitPuff : Actor
{
    int hitspawn;
    Default
	{
	RenderStyle "Add";
	Alpha 0.3;
	Scale 0.8;
	Radius 0;
	Height 0;
	+NOBLOCKMAP
	+NOGRAVITY
	+NOEXTREMEDEATH
	+FORCEXYBILLBOARD
	+DONTSPLASH
	-EXPLODEONWATER
	Gravity 0.01;
	DamageType "Bullet";
	Decal "BulletDecalNew1";
	}
	
	override void PostBeginPlay()
	{ Super.PostBeginPlay();
	  tex[0] = TexMan.CheckForTexture("GNSMK0",TexMan.Type_Sprite);
	  tex[1] = TexMan.CheckForTexture("LEYSO0",TexMan.Type_Sprite);
	  tex[2] = TexMan.CheckForTexture("LENYA0",TexMan.Type_Sprite);
	  hitspawn = random(2,4); }
	 
	TextureID tex[3];
	  
  States
  {
  Spawn:
    TNT1 A 0;
	TNT1 A 0 {
	for(int i=hitspawn;i>0;i--) A_SpawnItemEx("HM_BulletHit",0,0,0,frandom(20,20),frandom(-20,20),frandom(-20,20),random(-360,360),SXF_NOCHECKPOSITION,0);
	A_SpawnParticleEx
	( "90887b", tex[0], style: STYLE_Normal, flags:SPF_ROLL|SPF_RELATIVE, lifetime:random(5,25), size:random(10,45),
	  velx:frandom(-0.5,0.5), vely:frandom(-0.5,0.5), velz:frandom(-0.5,0.5),
	  accelx:frandom(-0.0015,0.0015), accely:frandom(-0.0015,0.0015), accelz:frandom(-0.0015,0.0015), startroll:random(-180,180));
	A_SpawnParticleEx
	( "", 
	  tex[1], 
	  style: STYLE_Add, 
	  flags:SPF_ROLL|SPF_RELATIVE|SPF_FULLBRIGHT, 
	  lifetime: random(1,5), 
	  size: random(15,45),
	  startalphaf: frandom(0.15,0.85),
	  sizestep: frandom(0.25,0.85), 
	  startroll: random(-180,180));
	A_SpawnParticleEx
	( "", 
	  tex[2], 
	  style: STYLE_Add, 
	  flags:SPF_ROLL|SPF_RELATIVE|SPF_FULLBRIGHT, 
	  lifetime: random(1,6), 
	  size: random(25,80),
	  startalphaf: frandom(0.35,0.85),
	  sizestep: frandom(0.35,0.85), 
	  startroll: random(-180,180));
	}
    Stop;
  Melee:
	TNT1 A 1;
	Stop;
  }
}

Class HM_NormalHitSpark : Actor
{
    Default
	{
	+CLIENTSIDEONLY
	+FORCEXYBILLBOARD
	+ROLLSPRITE
    +NOBLOCKMAP
    +NOTELEPORT
    -ALLOWPARTICLES
    +PUFFGETSOWNER
	RenderStyle "Normal";
	Scale 0.65;
	Height 1;
	Radius 1;
	Mass 1;
	-NOGRAVITY
	Gravity 0.25;
	}
	States
	{
	Spawn:
		TNT1 A 0;
	    TNT1 A 0 {
		for(int i=random(0,3);i>0;i--)
		 A_SpawnParticleEx
		 ( "#6e685e", TexMan.CheckForTexture("GNSMK0"), style: STYLE_Normal, flags: SPF_RELATIVE|SPF_ROLL, lifetime: random(15,55),
		 size: frandom(15,56), velx: frandom(-1,1), vely: frandom(-1,1), velz: frandom(-1,1), startalphaf: frandom(0.15,0.55),
		 fadestepf: -1/*-1*/, sizestep: frandom(0.0,1.5), startroll: random(-180,180), rollvel: frandom(-1,1));
		//for(int i=random(0,3);i>0;i--)
		 //A_SpawnParticleEx
		 //( "", TexMan.CheckForTexture("GNSML0"), style: STYLE_Normal, flags: SPF_RELATIVE|SPF_ROLL, lifetime: random(25,85),
		 //size: frandom(28,86), velx: frandom(-0.15,0.15), vely: frandom(-0.15,0.15), velz: frandom(-0.50,0.50), startalphaf: frandom(0.15,0.95),
		 //fadestepf: 1/*-1*/, sizestep: frandom(0.0,1.90), startroll: random(-180,180), rollvel: frandom(-1,1));
		for(int i=random(1,8);i>0;i--)
		 A_SpawnParticleEx(
		 "", TexMan.CheckForTexture("DUSTC0"), style: STYLE_Normal, flags: SPF_RELATIVE|SPF_ROLL, lifetime: random(20,60), size: frandom(1,13),
		 velx: frandom(-3,3), vely: frandom(-3,3), velz: frandom(-3,3), accelz: frandom(-0.3,-0.8), startalphaf: 1.0, sizestep: -0.2, startroll: random(-180,180), rollvel: random(0,4));
		}
		TNT1 A 0 A_SpawnItemEx("HM_HitPuff",0,0,0,0,0,0,0,SXF_NOCHECKPOSITION);
	    TNT1 A 0 A_StartSound("HMBarrel/Hit",100,CHANF_OVERLAP,0.85);
		Stop;
	}
}

//Base barrel shrapnel code by Bugsie
Class HM_BarrelShrapnel : Actor
{
  Default
  {
  Scale 1.0;
  BounceType "Doom";
  Speed 8;
  BounceFactor 0.3;
  Radius 6;
  Height 6;
  Gravity 0.95;
  Projectile;
  -NOGRAVITY
  +THRUACTORS
  +BOUNCEONFLOORS
  +BOUNCEONWALLS
  +ROLLSPRITE
  +CANNOTPUSH
  +MOVEWITHSECTOR
  }
  
  double xroll;
  
  override void PostBeginPlay()
  {
	frame = random(0, 3);
	scale *= frandom(0.8, 0.4);
	xroll = frandom(2, 35) * randompick(-1, 1);
  }
  
  override void Tick()
  {
	super.Tick();
	
	if (vel.z == 0 && (GetAge() > 150))
	{
		A_FadeOut(0.05);
		if (alpha <= 0) Destroy();
	}
  }

  States
  {
  LoadSprites:
	_BZZ ABCD 0;
  Spawn:
	#### # 0 A_QueueCorpse;
  SpawnGo:
	_BZZ # 1 { roll += xroll; }
	Loop;
  Bounce:
	#### # 0 A_StartSound("HMBarrel/DebrisHit",32);
	#### # 0 { roll /= 2; }
	Goto Spawn;
  Death:
  Crash:
	#### # 0 A_StartSound("HMBarrel/DebrisHit",32);
	_BZZ # -1 { roll = 0; }
	Stop;
  }
}

Class HM_GenericExplosionFlames : Actor
{
	Default
	{
	+NOINTERACTION
	+CLIENTSIDEONLY
	+THRUACTORS
	+FORCEXYBILLBOARD
	+ROLLSPRITE
	+ROLLCENTER
	Renderstyle "Add";
	Scale 1;
	+Bright
	}
	States
	{
	Spawn:
		TNT1 A 0;
		TNT1 A 0 A_QueueCorpse;
		TNT1 A 0 A_SetScale(frandom(1,2.5));
		TNT1 A 0 A_SetRoll(random(0,360));
        EXPL AA 2 BRIGHT;
        EXPL BCDEFGH 2 BRIGHT A_FadeOut(0.15);
		Stop;
	}
}

Class HM_WhiteExplosionFlames : HM_GenericExplosionFlames
{
Default
	{
	Scale 2;
	Speed 2;
	Translation "0:255=%[0,0,0]:[1.0,1.0,1.0]";
	}
	States
	{
    Spawn:
		TNT1 A 0;
		TNT1 A 0 A_SetRoll(random(0,360));
        EXPL AA 2 BRIGHT; //A_SpawnItem("PM_LargeRedFlare",0,0)
        EXPL BCDEF 3 BRIGHT;
        EXPL GH 2 BRIGHT;
        Stop;
	}
}

Class HM_SmallWhiteExplosionFlames : HM_GenericExplosionFlames
{
Default
	{
	Scale 1;
	Speed 2;
	Alpha 0.8;
	Translation "0:255=%[0,0,0]:[1.0,1.0,1.0]";
	}
	States
	{
    Spawn:
		TNT1 A 0;
		TNT1 A 0 A_SetScale(frandom(0.5,1));
		TNT1 A 0
		{
		alpha = frandom(0.3,0.8);
		}
		TNT1 A 0 A_SetRoll(random(0,360));
        EXPL AA random(2,4) A_FadeOut(0.04);
        EXPL BCDEF random(3,6) A_FadeOut(0.04);
        EXPL GH random(3,6) A_FadeOut(0.04);
        Stop;
	}
}

Class HM_MainExplosion : Actor
{
	Default
	{
	+NOINTERACTION
	+CLIENTSIDEONLY
	+FORCEXYBILLBOARD
	+ROLLSPRITE
	+ROLLCENTER
	+NOCLIP
	+BRIGHT
	//Renderstyle "Add";
	Scale 0.8;
	+Bright
	}
	States
	{
	Spawn:
		TNT1 A 0 NoDelay;
		TNT1 A 100 A_Jump(255,"T1","T4");
		Goto PlayOff;
	T1:
		TNT1 A 0 A_SetScale(scale.x*-1,scale.y);
		D4X1 ABC 1 Bright A_SetScale(scale.x-0.133,scale.y+0.133);
		Goto PlayOff;
	T4:
		D4X1 ABC 1 Bright A_SetScale(scale.x+0.133,scale.y+0.133);
		Goto PlayOff;
	PlayOff:
		D4X1 DEF 1 Bright;
		D4X1 GHI 1 Bright;
		D4X1 JKL 1 Bright;
		D4X1 MNO 1 Bright A_FadeOut(0.05);
		D4X1 PQR 1 Bright A_FadeOut(0.05);
		D4X1 ST 1 Bright A_FadeOut(0.05);
		D4X1 UV 1 Bright A_FadeOut(0.05);
		D4X1 WXYZ 1 Bright A_FadeOut(0.05);
		D4X2 ABCDE 1 Bright A_FadeOut(0.05);
		Stop;
	}
}

Class HM_BarrelSprayExplosion: Actor
{
	Default
	{
	+NOINTERACTION
	+CLIENTSIDEONLY
	+NOCLIP
	+BRIGHT
	Scale 1.5;
	}
	States
	{
	Spawn:
		TNT1 A 0 NODELAY A_Jump(128,"Type2");
	Type1:
		DB27 ABCDEFGHI 1;
		DB27 JKKLLMMNNOOPPQQRRSSTTUU 1;
		DB27 WWXXYYZZ 1 A_FadeOut(0.05);
		Wait;

	Type2:
		DB28 ABCDEFGHI 1;
		DB28 JKKLLMMNNOOPPQQRRSSTTUU 1;
		DB28 WWXXYYZZ 1 A_FadeOut(0.05);
		Wait;
	}
}

Class HM_BarrelFire: Actor
{
	Default
	{
	+NOINTERACTION
	+CLIENTSIDEONLY
	+NOCLIP
	+ROLLSPRITE
	+BRIGHT
	Scale 0.5;
	RenderStyle "Add";
	}
	States
	{
	Spawn:
	    TNT1 A 0;
		TNT1 A 0 A_SetRoll(random(0,360));
        FRPR CDEFGH 4 BRIGHT;
        Stop;
	}
}

Class HM_BarrelShockwave : Actor 
{
	Default
	{
	+NOBLOCKMAP
	+NOCLIP
	+NOGRAVITY
	+DONTSPLASH
	+NOTELEPORT
	+NOGRAVITY
	+FLATSPRITE
	+ZDOOMTRANS
	RenderStyle "Add";
	Alpha 0.70;
	Scale 0.1;
	}
	States{
		Spawn:
		TNT1 A 0;
		TNT1 A 0
		{
		A_SetScale(frandom(0.1,1.2));
		//A_SetRenderStyle(frandom(0.45,0.8),STYLE_Translucent);
		}
		TNT1 A 0 {A_SetRoll(Random(0,359)); A_SetPitch(Random(-90,90)); A_SetAngle(Random(0,359));}
		SKW_ A 1
		{
		A_SetScale(scale.x+0.25,scale.y+0.25);
		A_FadeOut(0.05);
		}
		Wait;
	}
}

Class HM_GroundIceSpawner : RandomSpawner
{
Default
	{
	DropItem "HM_GroundIce1", 255, 1;
	DropItem "HM_GroundIce2", 255, 1;
	}
}


Class HM_GroundIceBouncer : Actor
{
Default
	{
	Speed 0;
	Health 1;
	Radius 8;
	Height 2;
	Gravity 0.9;
	Damage 0;
   +MISSILE
   +GHOST
   +CLIENTSIDEONLY
   +NOTELEPORT
   +NOBLOCKMAP
   +FORCEXYBILLBOARD
   +NODAMAGETHRUST
   +MOVEWITHSECTOR
   -DONTSPLASH
   +DOOMBOUNCE
	Alpha 0.5;
	Scale 1;
	RenderStyle "Shaded";
	StencilColor "A0 FF FF";
	BounceFactor 0.01;
	}
    States
    {
     Spawn:
	 TNT1 A 0;
	 TNT1 A 10;
	 Stop;
	 Death:
	 TNT1 AA 0 Bright A_SpawnItemEx("HM_GroundIceSpawner", random(-70,70), random(-70,70), 0, 0, 0, 0, 0, SXF_CLIENTSIDE|SXF_NOCHECKPOSITION,70);
	 TNT1 A 50;
	 TNT1 AAAAAAAAAAAAAAAAAAAA 1 A_FadeOut(0.05);
  Stop;
    }
}

Class HM_GroundIce1 : Actor
{
Default
	{
	Monster;
	+SOLID
	+FLOORHUGGER
	+NOBLOOD
	+FORCEXYBILLBOARD
	+FRIENDLY
	-COUNTKILL
	+NOTARGET
	+THRUACTORS
	Gravity 500;
	Mass 10000000000;
	Radius 5;
	Health 500;
	Height 5;
	Speed 0;
	Scale 1.5;
	PainChance 256;
	}
	States
	{
	Spawn:
		TNT1 A 0;
		TNT1 A 0 A_SpriteOffset(0,15);
		TNT1 A 0 A_SetScale(random(1,3),random(1,3),AAPTR_DEFAULT,True);
		TNT1 A 0 A_Jump(128, "Flipped");
		ICR1 A 1;
		ICR1 A 6;
		ICR1 A 150;
		Goto Death;
	Flipped:
		TNT1 A 0 A_SetScale(scale.x*(-1),scale.y);
		ICR1 A 1;
		ICR1 A 6;
		ICR1 A 150;
		Goto Death;
	Death:
		ICR1 AAAAA 5 A_Fadeout(0.3);
		Stop;
	}
}

Class HM_GroundIce2 : Actor
{
Default
	{
	Monster;
	+SOLID
	+FLOORHUGGER
	+NOBLOOD
	+FORCEXYBILLBOARD
	+FRIENDLY
	-COUNTKILL
	+NOTARGET
	+THRUACTORS
	Gravity 500;
	Mass 10000000000;
	Radius 5;
	Health 500;
	Height 5;
	Speed 0;
	Scale 1.5;
	PainChance 256;
	}
	States
	{
	Spawn:
		TNT1 A 0;
		TNT1 A 0 A_SpriteOffset(0,15);
		TNT1 A 0 A_SetScale(random(1,3),random(1,3),AAPTR_DEFAULT,True);
		TNT1 A 0 A_Jump(128, "Flipped");
		ICR1 B 1;
		ICR1 B 6;
		ICR1 B 150;
		Goto Death;
	Flipped:
		TNT1 A 0 A_SetScale(scale.x*(-1),scale.y);
		ICR1 B 1;
		ICR1 B 6;
		ICR1 B 150;
		Goto Death;
	Death:
		ICR1 BBBBB 5 A_Fadeout(0.3);
		Stop;
	}
}

Class HM_CryoFrostCloud : Actor
{
Default
	{
	Radius 2;
	Height 2;
	Mass 0x7fffffff;
	+NOBLOCKMAP
	+NOGRAVITY
	+DROPOFF
	+NODAMAGETHRUST
	+DONTSPLASH
	+NOTELEPORT
	+SKYEXPLODE
	+FORCEXYBILLBOARD
	+FOILINVUL
	+CANBLAST
	+RIPPER
	+THRUACTORS
	+BLOODLESSIMPACT
	+BLOCKEDBYSOLIDACTORS
	+FORCERADIUSDMG
	+DOOMBOUNCE
	+ROLLSPRITE
	BounceFactor 0.3;
	WallBounceFactor 0.3;
	RenderStyle "Translucent";
	DamageType "Ice";
	Alpha 0.8;
	Scale 1.1;
	}
	States
	{
	Spawn:
		TNT1 A 1 A_SetRenderStyle(0.1,STYLE_Shaded);
		TNT1 A 0 A_SetRoll(random(0,360));
		TNT1 A 0 A_SetScale(0.1);
		GNSM I random(5,25) A_SetTranslucent(0.05);
		TNT1 A 0 A_SetScale(0.12);
		GNSM I random(5,25) A_SetTranslucent(0.1);
		TNT1 A 0 A_SetScale(0.18);
		GNSM I random(5,25) A_SetTranslucent(0.15);
		TNT1 A 0 A_SetScale(0.22);
		GNSM I random(5,25) A_SetTranslucent(0.2);
		TNT1 A 0 A_SetScale(0.18);
		GNSM I random(5,25) A_SetTranslucent(0.15);
		TNT1 A 0 A_SetScale(0.32);
		GNSM I random(5,25) A_SetTranslucent(0.3);
		TNT1 A 0 A_SetScale(0.45);
		TNT1 A 0 A_ChangeVelocity(0,0,0,CVF_REPLACE);
		GNSM IIIIIIIIIIIIIIIIIIII 8
		{
		A_Explode(Random(3,5),68);
		}
		GNSM IIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII random(8,20)
		{
		A_Explode(Random(3,5),68);
		A_RadiusGive("HM_CryoFreezingDamage",90,RGF_PLAYERS|RGF_MONSTERS,1);
		A_FadeOut(0.03);
		}
	Death:
		TNT1 A 1;
	Stop;
  }
}

Class HM_CryoFrostCloud2 : Actor
{
Default
	{
	Radius 2;
	Height 2;
	Mass 0x7fffffff;
	+NOBLOCKMAP
	+NOGRAVITY
	+DROPOFF
	+NODAMAGETHRUST
	+DONTSPLASH
	+NOTELEPORT
	+SKYEXPLODE
	+FORCEXYBILLBOARD
	+FOILINVUL
	+CANBLAST
	+RIPPER
	+THRUACTORS
	+BLOODLESSIMPACT
	+BLOCKEDBYSOLIDACTORS
	+FORCERADIUSDMG
	+DOOMBOUNCE
	+ROLLSPRITE
	BounceFactor 0.3;
	WallBounceFactor 0.3;
	RenderStyle "Translucent";
	DamageType "Ice";
	Alpha 0.8;
	Scale 1.1;
	}
	States
	{
	Spawn:
		TNT1 A 1 A_SetRenderStyle(0.1,STYLE_Shaded);
		TNT1 A 0 A_SetRoll(random(0,360));
		TNT1 A 0 A_SetScale(0.3);
		GNSM I random(2,15) A_SetTranslucent(0.4);
		TNT1 A 0 A_SetScale(0.3);
		GNSM I random(2,15) A_SetTranslucent(0.45);
		TNT1 A 0 A_SetScale(0.35);
		GNSM I random(2,15) A_SetTranslucent(0.5);
		TNT1 A 0 A_SetScale(0.35);
		GNSM I random(2,15) A_SetTranslucent(0.6);
		TNT1 A 0 A_SetScale(0.40);
		GNSM I random(2,15) A_SetTranslucent(0.7);
		TNT1 A 0 A_SetScale(0.45);
		GNSM I random(2,15) A_SetTranslucent(0.75);
		TNT1 A 0 A_SetScale(0.50);
		TNT1 A 0 A_ChangeVelocity(0,0,0,CVF_REPLACE);
		GNSM IIIIIIIIIIIIIIIIIIII 8
		{
		A_Explode(Random(3,5),68);
		}
		GNSM IIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII random(4,10)
		{
		A_Explode(Random(3,10),68);
		A_RadiusGive("HM_CryoFreezingDamage",90,RGF_PLAYERS|RGF_MONSTERS,1);
		A_FadeOut(0.03);
		}
	Death:
		TNT1 A 1;
	Stop;
  }
}

Class HM_CryoFreezingDamage : PowerSpeed
{
	Default
	{
		Powerup.Duration -2;
		Speed 0.25;
		Inventory.Icon "";
		Powerup.Color "f3feff", 0.4;
		+POWERSPEED.NOTRAIL;
		+INVENTORY.NOSCREENBLINK;
	}
	Override void DoEffect()
	{
		Super.DoEffect();
		if(owner.health > 0 && !IsFrozen())
		{
			if(owner && !IsFrozen() && owner.health > 0)
			{
			if(!random(0,3))
			{
				owner.A_SpawnParticleEx
				(   
				"",
				TexMan.CheckForTexture("GNSMI0"),
				style: STYLE_Normal,
				flags: SPF_ROLL|SPF_RELATIVE,
				lifetime: random(30,80),
				size: frandom(15,95),
				angle: 0,
				xoff: frandom(-4,4),
				yoff: frandom(-4,4),
				zoff: frandom(5,40),
				velx: frandom(-1,1),
				vely: frandom(-1,1),
				velz: frandom(-1,1),
				accelz: frandom(-0.01,-0.2),
				startalphaf: frandom(0.1,0.6),
				fadestepf: -1,
				sizestep: frandom(1,4.5),
				startroll: random(-180,180),
				rollvel: frandom(-2,2),
				rollacc: 0
				);
			}
			if(!random(0, 40))
			{
            owner.A_StartSound("misc/freeze", 34, pitch:frandom(0.9, 1.1));
			}
			if(!random(0, 10) && !IsFrozen() && owner.health > 0)
			{
			owner.A_Explode(1,10,damagetype:"Ice");
			}
		}
	}
}
}

Class HM_CryoFreezing : PowerSpeed
{
	Default
	{
		Powerup.Duration -6;
		Speed 0.4;
		Inventory.Icon "";
		Powerup.Color "f3feff", 0.1;
		+POWERSPEED.NOTRAIL;
		+INVENTORY.NOSCREENBLINK;
	}
	Override void DoEffect()
	{
		Super.DoEffect();
		if(owner.health > 0 && !IsFrozen())
		{
			if(!random(0,2))
			{
			owner.A_SpawnParticleEx
			(   
			"",
			TexMan.CheckForTexture("GNSMI0"),
			style: STYLE_Normal,
			flags: SPF_ROLL|SPF_RELATIVE,
			lifetime: random(60,120),
			size: frandom(5,70),
			angle: 0,
			xoff: frandom(-4,4),
			yoff: frandom(-4,4),
			zoff: frandom(2,25),
			velx: frandom(-1,1),
			vely: frandom(-1,1),
			velz: frandom(-1,1),
			accelz: frandom(-0.01,-0.2),
			startalphaf: frandom(0.1,0.32),
			fadestepf: -1,
			sizestep: frandom(1,4.5),
			startroll: random(-180,180),
			rollvel: frandom(-2,2),
			rollacc: 0
			);
			}
		}
	}
}